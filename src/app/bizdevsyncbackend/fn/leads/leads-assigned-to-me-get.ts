/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Lead } from '../../models/lead';

export interface LeadsAssignedToMeGet$Params {
  page?: number;
  limit?: number;
}

export function leadsAssignedToMeGet(http: HttpClient, rootUrl: string, params?: LeadsAssignedToMeGet$Params, context?: HttpContext): Observable<StrictHttpResponse<{
'count'?: number;
'rows'?: Array<Lead>;
}>> {
  const rb = new RequestBuilder(rootUrl, leadsAssignedToMeGet.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      'count'?: number;
      'rows'?: Array<Lead>;
      }>;
    })
  );
}

leadsAssignedToMeGet.PATH = '/leads/assigned-to-me';
